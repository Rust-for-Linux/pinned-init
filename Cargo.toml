[package]
name = "simple-safe-init"
version = "0.0.0"
edition = "2021"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]

[features]
default = ["std"]
std = []
alloc = []
never_type = []

[[example]]
name = "linked_list"
required-features = ["never_type"]

[[example]]
name = "mutex"
required-features = ["never_type"]

[dev-dependencies]
libc = "0.2"
